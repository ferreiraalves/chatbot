{
  "name": "blipchat",
  "version": "1.0.0",
  "description": "Chatbot com maquina de estados utilizando o blip.ai",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ferreiraalves/chatbot.git"
  },
  "keywords": [
    "chatbot",
    "blip"
  ],
  "author": "ferreiraalves",
  "license": "MIT",
  "dependencies": {
    "codacy-coverage": "^3.4.0",
    "express": "4.15.2",
    "istanbul": "^0.4.5",
    "lime-transport-websocket": "^2.0.2",
    "messaginghub-client": "^3.3.0",
    "newrelic": "^2.0.2"
  },
  "scripts": {
    "test-with-coverage": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | codacy-coverage && rm -rf ./coverage"
  }
}
